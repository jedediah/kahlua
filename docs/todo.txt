* Write tests for setfenv and getfenv
* Investigate whether environment should be in thread or state
* Speed up the report building
* Add testing for gsub - especially for invalid repl (i.e. bad usage of %)

* Convert old tests to new system

* Implement require, module and loading of bytecode files
* Try to get metalua to work with kahlua
* Create a proper manual
* Add array part of lua table
* Implement a lua compiler

* Major change: Implement metatables per object instead of per class for userdata.

